#include <iostream>

#include "merkle_tree.hpp"
/*
MerkleTree::MerkleTree() : leaves_(), nodes_(), root_(nullptr) {}


void MerkleTree::AddLeaf(byte* leaf) {
    leaves_.push_back(leaf);
}


void MerkleTree::Build() {
      if (leaves_.empty()) {
            std::cerr << "No leaves to build the tree!" << std::endl;
            return;
      }
}


void MerkleTree::Print() {
      if (root_ == nullptr) {
            std::cerr << "No root to print!" << std::endl;
            return;
      }
}

*/
